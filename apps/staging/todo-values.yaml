apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: todo-release
  namespace: prod
spec:
  chart:
    spec:
      version: ">=2.0.0"
  values:
    ingress:
      hosts:
        - host: todo.staging
          paths:
            - path: /
              pathType: ImplementationSpecific
